// 生成的内容接口
export interface GeneratedContent {
  activityId: string
  imageUrl?: string
  story?: {
    time: string
    name: string
    description: string
  }
  isLoading: boolean
  error?: string
} 